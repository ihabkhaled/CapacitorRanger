import { APP_PATHS } from '@/shared/config';

export function aboutPath(): string {
  return APP_PATHS.about;
}

export function featuresPath(): string {
  return APP_PATHS.features;
}

export function faqPath(): string {
  return APP_PATHS.faq;
}

export function contactPath(): string {
  return APP_PATHS.contact;
}
